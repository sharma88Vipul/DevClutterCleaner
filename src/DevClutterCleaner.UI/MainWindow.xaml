<Window x:Class="DevClutterCleaner.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DevClutterCleaner"
        Height="360"
        Width="560"
        MinHeight="320"
        MinWidth="480">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="DevClutterCleaner"
                       FontSize="24"
                       FontWeight="SemiBold" />
            <TextBlock Text="MVP scanner for developer cache usage"
                       Foreground="#5C6670"
                       Margin="0,4,0,0" />
        </StackPanel>

        <Button Grid.Row="1"
                Content="Scan NuGet Cache"
                Width="150"
                Height="34"
                Margin="0,24,0,16"
                HorizontalAlignment="Left"
                Click="ScanButton_Click" />

        <Border Grid.Row="2"
                BorderBrush="#D7DCE1"
                BorderThickness="1"
                Padding="16">
            <StackPanel>
                <TextBlock Text="Results"
                           FontWeight="SemiBold"
                           Margin="0,0,0,12" />
                <TextBlock x:Name="NuGetCacheResultText"
                           Text="NuGet Cache: Not scanned"
                           Margin="0,0,0,6" />
                <TextBlock x:Name="TotalReclaimableResultText"
                           Text="Total reclaimable space: Not scanned" />
                <TextBlock x:Name="StatusText"
                           Foreground="#8A1C1C"
                           TextWrapping="Wrap"
                           Margin="0,12,0,0" />
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="3"
                   Text="This version only scans. It does not delete files."
                   Foreground="#5C6670"
                   Margin="0,16,0,0" />
    </Grid>
</Window>
